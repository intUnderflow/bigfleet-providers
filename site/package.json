{
  "name": "bigfleet-providers-site",
  "version": "0.0.1",
  "private": true,
  "type": "module",
  "scripts": {
    "sync": "node scripts/sync-providers.mjs",
    "dev": "npm run sync && astro dev",
    "build": "npm run sync && astro build",
    "preview": "astro preview",
    "astro": "astro"
  },
  "dependencies": {
    "@astrojs/starlight": "^0.30.3",
    "astro": "^5.0.0",
    "sharp": "^0.33.5"
  }
}
