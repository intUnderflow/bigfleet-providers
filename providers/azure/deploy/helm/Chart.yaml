apiVersion: v2
name: bigfleet-azure
description: BigFleet CapacityProvider for Microsoft Azure — one release per Azure region.
type: application
# Chart version; bump on chart changes.
version: 0.1.0
# Tracks the provider binary; the image tag is set in values.yaml.
appVersion: "0.1.0"
keywords:
  - bigfleet
  - capacity-provider
  - azure
  - virtual-machines
home: https://github.com/intUnderflow/bigfleet-providers/tree/main/providers/azure
sources:
  - https://github.com/intUnderflow/bigfleet-providers
