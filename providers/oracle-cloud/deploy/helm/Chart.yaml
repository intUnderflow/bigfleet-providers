apiVersion: v2
name: bigfleet-oracle-cloud
description: BigFleet CapacityProvider for Oracle Cloud Infrastructure (OCI) Compute — one release per region.
type: application
# Chart version; bump on chart changes.
version: 0.1.0
# Tracks the provider binary; the image tag is set in values.yaml.
appVersion: "0.1.0"
keywords:
  - bigfleet
  - capacity-provider
  - oracle-cloud
  - oci
  - compute
home: https://github.com/intUnderflow/bigfleet-providers/tree/main/providers/oracle-cloud
sources:
  - https://github.com/intUnderflow/bigfleet-providers
