apiVersion: v2
name: bigfleet-upcloud
description: BigFleet CapacityProvider for UpCloud cloud servers — one release per zone.
type: application
# Chart version; bump on chart changes.
version: 0.1.0
# Tracks the provider binary; the image tag is set in values.yaml.
appVersion: "0.1.0"
keywords:
  - bigfleet
  - capacity-provider
  - upcloud
  - cloud-servers
home: https://github.com/intUnderflow/bigfleet-providers/tree/main/providers/upcloud
sources:
  - https://github.com/intUnderflow/bigfleet-providers
