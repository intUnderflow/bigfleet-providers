apiVersion: v2
name: bigfleet-ovhcloud
description: BigFleet CapacityProvider for OVHcloud Public Cloud (OpenStack) — one release per region.
type: application
# Chart version; bump on chart changes.
version: 0.1.0
# Tracks the provider binary; the image tag is set in values.yaml.
appVersion: "0.1.0"
keywords:
  - bigfleet
  - capacity-provider
  - ovhcloud
  - openstack
  - public-cloud
home: https://github.com/intUnderflow/bigfleet-providers/tree/main/providers/ovhcloud
sources:
  - https://github.com/intUnderflow/bigfleet-providers
