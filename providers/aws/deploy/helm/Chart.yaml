apiVersion: v2
name: bigfleet-aws
description: BigFleet CapacityProvider for AWS EC2 — one release per AWS region.
type: application
# Chart version; bump on chart changes.
version: 0.1.0
# Tracks the provider binary; the image tag is set in values.yaml.
appVersion: "0.1.0"
keywords:
  - bigfleet
  - capacity-provider
  - aws
  - ec2
home: https://github.com/intUnderflow/bigfleet-providers/tree/main/providers/aws
sources:
  - https://github.com/intUnderflow/bigfleet-providers
