apiVersion: v2
name: bigfleet-latitude
description: BigFleet CapacityProvider for Latitude.sh on-demand bare metal — one release per site.
type: application
# Chart version; bump on chart changes.
version: 0.1.0
# Tracks the provider binary; the image tag is set in values.yaml.
appVersion: "0.1.0"
keywords:
  - bigfleet
  - capacity-provider
  - latitude
  - latitude.sh
  - bare-metal
home: https://github.com/intUnderflow/bigfleet-providers/tree/main/providers/latitude
sources:
  - https://github.com/intUnderflow/bigfleet-providers
