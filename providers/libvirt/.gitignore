# A bare `go build .` inside this module drops a binary named after the module
# dir (`libvirt`); build via `make build-libvirt` (writes to repo-root bin/)
# instead. Ignore the stray so it can never be committed again.
/libvirt
