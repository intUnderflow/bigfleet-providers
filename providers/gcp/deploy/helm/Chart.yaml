apiVersion: v2
name: bigfleet-gcp
description: BigFleet CapacityProvider for Google Compute Engine (GCE) — one release per GCP region.
type: application
# Chart version; bump on chart changes.
version: 0.1.0
# Tracks the provider binary; the image tag is set in values.yaml.
appVersion: "0.1.0"
keywords:
  - bigfleet
  - capacity-provider
  - gcp
  - gce
  - compute-engine
home: https://github.com/intUnderflow/bigfleet-providers/tree/main/providers/gcp
sources:
  - https://github.com/intUnderflow/bigfleet-providers
