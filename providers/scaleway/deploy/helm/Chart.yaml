apiVersion: v2
name: bigfleet-scaleway
description: BigFleet CapacityProvider for Scaleway — one release per region/zone.
type: application
# Chart version; bump on chart changes.
version: 0.1.0
# Tracks the provider binary; the image tag is set in values.yaml.
appVersion: "0.1.0"
keywords:
  - bigfleet
  - capacity-provider
  - scaleway
  - instances
  - elastic-metal
home: https://github.com/intUnderflow/bigfleet-providers/tree/main/providers/scaleway
sources:
  - https://github.com/intUnderflow/bigfleet-providers
